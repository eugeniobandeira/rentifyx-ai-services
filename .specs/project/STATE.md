# State

## Decisions

- Use a multi-Lambda repo with shared event contracts and isolated IAM roles.
- Keep the repo event-only and avoid synchronous API exposure.
- Treat duplicate/fraud detection as deferred and explicitly scaffolded.
- Follow the same repo convention as the existing RentifyX services: use `src`, `tests`, `iac`, `docs`, and `.specs`.
- The Shared project's C# namespace is `RentifyxAiServices.SharedKernel`, not `RentifyxAiServices.Shared` — the folder/project/csproj name stays `Shared` (matches convention), only the namespace differs, to avoid CA1716 (namespace segment "Shared" collides with a reserved keyword in other .NET languages, e.g. VB.NET's `Shared` modifier). No sibling repo (`rentifyx-identity-api`, `-communications-api`, `-asset-registry-api`, `-platform`) has an analogous `Shared` project or `Directory.Build.props`/`TreatWarningsAsErrors` setup to mirror — this is a local, first-of-its-kind decision in this repo. If a future sibling repo adds a `Shared`/common project under the same strict analyzer settings, apply the same `SharedKernel` naming rather than suppressing CA1716.

## Current Status

- Foundation scaffold created for the repository.
- Solution and initial .NET projects were created and wired into the solution.
- Infrastructure folder was normalized to `iac/` to match the existing service convention.
- Verified with a real build: `dotnet build RentifyxAiServices.slnx` completed successfully.

- Fixed naming drift: `tests/RentifyX.AiServices.*` renamed to `RentifyxAiServices.*.Tests` to match `src` and sibling repos (`RentifyxIdentity`, `RentifyxCommunications`).
- Fixed structural drift: `Models/`, `Prompts/`, and `Shared/{Aws,Events,Kafka,Idempotency,Observability}` moved inside their owning `.csproj` project folders (were sitting one level above, outside the project).
- E-01 closed via `.specs/features/e01-foundation/` (spec.md + tasks.md), tlc-spec-driven workflow:
  - 4 xUnit test projects scaffolded and wired into `RentifyxAiServices.slnx` (Moderation, Enrichment, Shared, Integration), one placeholder test each.
  - `tests/Directory.Build.props` added — suppresses CA1707/CA1716/CA1859/CA1305/CA1001 for test conventions (mirrors `rentifyx-identity-api` pattern).
  - `.github/workflows/ci.yml` added: restore → build → test on push/PR to `main`. Gate is pass/fail only, no coverage threshold (user decision 2026-07-22, overrides original plan T-008).
  - `iac/modules/iam-roles` written: one role + scoped policy per Lambda (moderation, enrichment, dedupe), no shared execution role. `terraform fmt -check` passes; `terraform validate` blocked in this sandbox by provider-download failure (insufficient system resources for `terraform init`), not an HCL error — needs re-validation in an environment that can reach the Terraform registry.
  - `docs/adr/ADR-AI-001-independent-deploy-native-aot.md` and `docs/adr/ADR-AI-002-iam-isolation.md` written and accepted.
- Gate check: `dotnet test RentifyxAiServices.slnx` → 4/4 projects pass, 0 failures.
- E-02 (Moderation Pipeline) implemented via `.specs/features/e02-moderation-pipeline/` (spec.md + design.md + tasks.md), tlc-spec-driven workflow, 2026-07-22:
  - Full handler stack built: `AssetKeyConventionFilter`, `DynamoDbIdempotencyStore` (Shared), `RekognitionModerationClient` (retry/backoff on throttling), `ThresholdEvaluator` (60/90 boundaries), `KafkaEventPublisher<T>` (Shared, generic — reusable by Enrichment), `KafkaModerationEventPublisher`, `ModerationService` orchestrator, `ModerationHandler` Lambda entrypoint. Replaces the old placeholder `Class1.cs`.
  - `iac/modules/iam-roles`'s `moderation` policy extended with DynamoDB PutItem, MSK IAM-auth publish, SQS SendMessage — gap found during design (role previously only had Rekognition + S3 read). New `iac/modules/review-queue` module: SQS review queue + its DLQ, separate Rekognition-failure DLQ, CloudWatch depth alarm.
  - `docs/adr/ADR-AI-003-moderation-thresholds.md` and `ADR-AI-004-hybrid-moderation-strategy.md` written and accepted.
  - Several NuGet package pins in `Directory.Packages.props` were stale (removed from NuGet.org) and had to be bumped to real current versions during implementation: `AWSSDK.Rekognition` 4.0.14.0→4.0.100.5, `Amazon.Lambda.Core` 2.7.0→3.1.1, `Amazon.Lambda.S3Events` 2.2.0→4.0.0, `Amazon.Lambda.Serialization.SystemTextJson` 2.6.0→3.0.0, `Amazon.Lambda.TestUtilities` 2.3.0→3.0.0. New pins added: `AWSSDK.SQS`, `AWSSDK.S3`. All versions confirmed against the live NuGet.org flat-container API before pinning, not guessed.
  - `Amazon.Lambda.S3Events` 4.0.0 flattened its old nested `S3EventNotification` wrapper class — the real type is `Amazon.Lambda.S3Events.S3Event.S3EventNotificationRecord` (nested under `S3Event`), confirmed via the package's XML doc comments, not the (misleading) older API shape assumed in the original task breakdown.
  - Gate check: `dotnet test` across Shared.Tests (4), Moderation.Tests (24), Enrichment.Tests (1), IntegrationTests (1 placeholder) → 30/30 pass. Full solution `dotnet build --configuration Release` clean, 0 warnings/errors.
  - `tests/RentifyxAiServices.IntegrationTests/ModerationPipelineTests.cs` (LocalStack S3/DynamoDB + real Kafka via Testcontainers) verified 2026-07-22 with Docker running → 3/3 pass. Fixed three real bugs surfaced only by a live run: `KafkaEventPublisher<T>` was serializing `Verdict` as its numeric ordinal instead of its name (added `JsonStringEnumConverter` — cross-repo Kafka consumers would otherwise have had to hardcode enum ordinals); the test's S3/DynamoDB clients had both `ServiceURL` and `RegionEndpoint` set, which routed requests to real AWS instead of LocalStack (fixed via `AuthenticationRegion`); Kafka consumers subscribed before the topic existed (fixed by pre-creating topics via `AdminClient`).

- **Cross-repo infra correction (2026-07-22, post-E-02):** discovered the sibling `rentifyx-platform` repo (`C:\Users\Eugenio\Projects\study\rentifyx-platform`) owns shared infra — VPC, and a **self-hosted Kafka broker** (EC2, KRaft, PLAINTEXT, port 9092), not Amazon MSK. E-02's design/tasks assumed MSK IAM auth (not knowing this repo existed at the time); confirmed against `rentifyx-identity-api`'s already-working Terraform that the real pattern is: read the bootstrap address once via `terraform_remote_state` + `aws_ssm_parameter` at deploy time, inject as an env var — no runtime IAM `kafka-cluster:*` permission, no SASL/IAM auth. Removed the incorrect `KafkaPublishVerdict` IAM statement (and its now-meaningless `moderation_kafka_cluster_arn`/`moderation_kafka_topic_arn` variables) from `iac/modules/iam-roles/main.tf`; `terraform validate` re-run clean. Docs corrected: `CLAUDE.md` (new "Cross-repo infra" section), `.specs/codebase/INTEGRATIONS.md`, `.specs/codebase/STRUCTURE.md`, and a correction note added atop E-02's `design.md` (left the rest as historical record rather than rewritten).
- `terraform validate` for `iac/modules/iam-roles` and `iac/modules/review-queue` re-run 2026-07-22 with registry access available — both pass clean (previously only `fmt -check` had been verified).
- `iac/modules/lambda-moderation` written 2026-07-23: `aws_lambda_function.moderation` wired to `iam-roles`' `moderation_role_arn` (ADR-AI-002, no shared role), VPC-attached via a new egress-only `aws_security_group.moderation_lambda` plus a `terraform_remote_state` read of `rentifyx-platform`'s state (same S3 backend/bucket pattern as `rentifyx-identity-api`'s `iac/terraform/main.tf`) to resolve `vpc_id` / `public_subnets[0]` and the Kafka bootstrap address (`kafka_ssm_parameter_path` → `aws_ssm_parameter.kafka_bootstrap_servers`, wrapped in `try()`). All six `ModerationHandler` env vars (`IDEMPOTENCY_TABLE_NAME`, `KAFKA_BOOTSTRAP_SERVERS`, `KAFKA_MODERATED_TOPIC`, `KAFKA_PENDING_REVIEW_TOPIC`, `REVIEW_QUEUE_URL`, `FAILURE_DLQ_URL`) are injected via `environment.variables`. Exposes `function_arn`/`invoke_arn`/`function_name`/`security_group_id` outputs for the (separately in-progress) `iac/modules/s3-trigger` module to wire its `aws_lambda_permission` against — this module does not touch S3 bucket notifications itself. `terraform fmt -check` and `terraform init && terraform validate` both pass clean. Added `iac/modules/review-queue`'s missing `moderation_failure_dlq_url` output (only the ARN existed; `sqs:SendMessage`/env var wiring needs a URL) — small additive change to an existing module, re-validated clean.
  - Gap found, not resolved here: no DynamoDB table resource/module exists anywhere in this repo's `iac/` for the idempotency table — `idempotency_table_name` is taken as a plain input variable on this module rather than this module inventing a table schema. Needs its own module (or an addition to an existing one) before this can be applied for real.
  - Gap found, not resolved here: `rentifyx-platform`'s root `outputs.tf` only exposes `public_subnets` (not `private_subnets`), so the Lambda is VPC-attached into a public subnet, matching `rentifyx-identity-api`'s existing EC2 pattern exactly rather than inventing a private-subnet path platform doesn't currently output. Worth revisiting once platform exposes private subnets, since a Lambda invoked by S3 doesn't need a public IP.
  - Not yet wired: nothing in `iac/` currently instantiates `lambda-moderation` (or `iam-roles`/`review-queue`) as modules of a root Terraform config — each is still validated standalone. A root `main.tf` tying prefix/outputs together across modules doesn't exist yet.
- `iac/modules/s3-trigger` written 2026-07-23 (parallel to `lambda-moderation`, in an isolated worktree, merged after both landed): `aws_s3_bucket_notification` + `aws_lambda_permission` wiring the Moderation Lambda to `s3:ObjectCreated:*`. `filter_prefix`/`filter_suffix` are plain variables with empty-string defaults — deliberately not hardcoding the unconfirmed `assets/{ownerId}/{assetId}/{filename}` convention (G-001). Bucket and Lambda are taken as input variables; this module owns neither. `terraform fmt`/`init`/`validate` clean.
- **Build regression found and fixed (2026-07-23):** `dotnet build` was broken solution-wide — root cause was the same-day "chore: code cleanup" commit (`96ca431`), which renamed the Shared project's namespace `RentifyxAiServices.SharedLibrary` → `RentifyxAiServices.Shared` (introducing CA1716, since "Shared" collides with a reserved keyword in other .NET languages) and split the Moderation project into sub-namespaces (`KeyConvention`/`Rekognition`/`Threshold`/`Publishing`) while stripping the `using` directives that made cross-namespace types visible — never rebuilt before committing. Fixed by renaming the namespace to `RentifyxAiServices.SharedKernel` (physical folder/csproj name `Shared` untouched) and restoring visibility via global `<Using>` items in each affected csproj (`Moderation`, `Moderation.Tests`, `Shared.Tests`, `IntegrationTests`) rather than per-file usings. Verified: `dotnet build --configuration Release` clean, `dotnet test` 33/33 pass across all 4 projects including 4/4 `IntegrationTests` against real Docker (LocalStack + Kafka via Testcontainers, 3m37s). See Decisions above for the naming rationale.
- `iac/terraform/` root config written 2026-07-23 (`backend.tf`/`main.tf`/`variables.tf`/`outputs.tf`), mirroring `rentifyx-identity-api`'s root layout (empty `backend "s3" {}` skeleton + CLI-flag init, `aws` provider with `default_tags`, `local.prefix = "${app_name}-${environment}"`). Instantiates `iam-roles`, `review-queue`, `lambda-moderation`, `s3-trigger` together, wiring each module's outputs into the next's inputs (e.g. `iam_roles.moderation_role_arn` → `lambda_moderation.moderation_role_arn`, `lambda_moderation.function_arn`/`function_name` → `s3_trigger`'s inputs). `terraform fmt`/`init -backend=false`/`validate` all pass clean. This closes the roadmap's "Event contracts and integration baseline" item at the Terraform-composition level.
  - Cannot `terraform apply` yet, only `validate` — several root variables have no default because the real values don't exist anywhere yet: `media_bucket_id`/`media_bucket_arn` (bucket isn't provisioned in any repo, still blocked on G-001), `lambda_package_path` (no CI build step produces a zip yet), `idempotency_table_name`/`idempotency_table_arn` (no DynamoDB table resource exists), `bedrock_model_arn` (Enrichment/E-03 not implemented). All pre-existing, documented gaps, not introduced by this root config.
- **E-03 (Enrichment Pipeline) implemented via `.specs/features/e03-enrichment-pipeline/` (spec.md + design.md + tasks.md), tlc-spec-driven workflow, 2026-07-23:**
  - Contract change: `AssetMediaModerated` bumped to `SchemaVersion = 2`, adds `Bucket`/`Key` so Enrichment can fetch the same S3 object Moderation scanned without a synchronous cross-service lookup (additive, per CLAUDE.md convention). `ModerationService` updated to populate both; existing 24 Moderation tests updated, none deleted.
  - New event `AssetEnrichmentSuggested(AssetId, Description, Tags, Timestamp, SchemaVersion)`.
  - Full handler stack built, mirroring Moderation's shape: `EnrichmentHandler` (entrypoint, Kafka-triggered via `Amazon.Lambda.KafkaEvents.KafkaEvent`, not S3) → `EnrichmentService` (orchestrator: skip non-`Approved` verdicts, idempotency via a *separate* DynamoDB table keyed `enrichment:{assetId}`, S3 fetch, Bedrock call, publish-or-DLQ) → `BedrockEnrichmentClient` (Bedrock Converse API against Claude Sonnet 5, tool-forced structured output, retry/backoff on throttling — ADR-AI-005/006) → `KafkaEnrichmentEventPublisher`.
  - Package pin fixes: `AWSSDK.BedrockRuntime` `4.0.14.0` (didn't exist on NuGet) → `4.0.100.6`, confirmed against the live flat-container API. Added `Amazon.Lambda.KafkaEvents` `3.0.0` (new dependency).
  - Bedrock API research: Context7 (`/aws/aws-sdk-net`) confirmed the Converse API is the right modern surface but couldn't resolve exact field names for `ContentBlock`/`ImageBlock`/`ToolConfiguration`/etc — fell back to the restored `AWSSDK.BedrockRuntime` 4.0.100.6 / `AWSSDK.Core` 4.0.100.8 package XML doc files (confirmed `ImageBlock.Format`/`.Source.Bytes`, `ContentBlock.Image`/`.Text`/`.ToolUse`, `ToolConfiguration.Tools`/`.ToolChoice`, `Document.AsDictionary/AsList/AsString`). `Amazon.Lambda.KafkaEvents`' exact `KafkaEvent.Records`/`KafkaEventRecord.Value` types weren't in its XML doc either (no member-type info) — resolved by writing the code with a best-confirmed guess and letting the compiler's real error messages correct it (`Records` is `Dictionary<string, IList<KafkaEventRecord>>`, `Value` is an already-decoded `MemoryStream`, not base64 `string`).
  - Model: Claude Sonnet 5 via Bedrock cross-region inference profile `us.anthropic.claude-sonnet-5` (confirmed current via web search during Design, not fabricated).
  - Cost/safety guardrails (ADR-AI-006): `MaxTokens` capped at 1024; system prompt kept structurally separate from the image content block (role separation); output forced through a named tool (`suggest_enrichment`) with a JSON schema — any non-matching response is a hard failure routed to a dedicated Enrichment failure DLQ, never a best-effort parse or a malformed publish.
  - Two sub-agents (T5, T7) ran in parallel directly in the main working directory (not worktrees — worktree isolation had repeatedly snapshotted a stale pre-CA1716-fix commit earlier in the session, see the build-regression entry above; running in-place avoided re-triggering that). T6 (the Bedrock research/implementation task) stalled mid-research in its own sub-agent run (600s no progress) and was finished directly rather than retried.
  - Gate check: `dotnet build --configuration Release` clean throughout. `dotnet test` — Shared.Tests 4/4, Moderation.Tests 24/24 (unchanged count, contract-updated), Enrichment.Tests 15/15 (new), IntegrationTests 8/8 (4 pre-existing Moderation + 4 new `EnrichmentPipelineTests`, verified 2026-07-23 against real Docker, Testcontainers.LocalStack + Testcontainers.Kafka, 4m03s). Bedrock is stubbed in the integration test — LocalStack community doesn't support it, same posture Rekognition would need if LocalStack ever dropped its support.
  - `docs/adr/ADR-AI-005-bedrock-converse-api.md` and `ADR-AI-006-enrichment-cost-and-prompt-safety.md` written and accepted.
  - Not done (deliberately out of this feature's tasks.md scope, tracked as follow-up): `iac/modules/{lambda-enrichment,kafka-event-source-mapping}`, a new enrichment idempotency table module, a new enrichment failure-DLQ module, and extending `iac/modules/iam-roles`' `enrichment` policy (currently only `bedrock:InvokeModel`) with S3/DynamoDB/SQS permissions. Same order E-02 shipped in — code before IaC.

- **E-03b (Enrichment Pipeline IaC) implemented via `.specs/features/e03b-enrichment-iac/` (spec.md + design.md + tasks.md), tlc-spec-driven workflow, 2026-07-24:**
  - Closes the E-03 follow-up gap: `iac/modules/lambda-enrichment` (VPC-attached Enrichment Lambda, mirrors `lambda-moderation`'s remote-state/SG/VPC-attach block, env vars confirmed against `EnrichmentHandler.BuildService()`: `ENRICHMENT_IDEMPOTENCY_TABLE_NAME`, `ENRICHMENT_FAILURE_DLQ_URL`, `BEDROCK_REGION`, `KAFKA_ENRICHMENT_SUGGESTED_TOPIC`, `KAFKA_BOOTSTRAP_SERVERS`) and `iac/modules/kafka-event-source-mapping` (new — first self-managed Kafka `aws_lambda_event_source_mapping` in this repo, shape confirmed via Context7 against `/hashicorp/terraform-provider-aws`, not guessed: `self_managed_event_source`, `self_managed_kafka_event_source_config`, `source_access_configuration` of type `VPC_SUBNET`/`VPC_SECURITY_GROUP`, no auth-type block since the broker is PLAINTEXT).
  - New generic `iac/modules/dynamodb-table` module (single partition key, optional TTL, `PAY_PER_REQUEST`) — closes the STATE.md gap that no DynamoDB table resource existed anywhere in `iac/` for either Lambda. Deliberately generic, not Enrichment-specific, so Moderation's still-unbacked `idempotency_table_name` variable can adopt the same module later (that adoption itself is still not done — Moderation's root variable is untouched by this feature).
  - `iac/modules/review-queue` extended with a bare `enrichment_failure_dlq` SQS queue (same no-redrive-policy shape as the existing `moderation_failure_dlq`) plus its URL/ARN outputs.
  - `iac/modules/iam-roles`' `enrichment` policy extended from one statement (`BedrockInvoke`) to four: added `S3Read` (reuses the same `media_bucket_arn` variable moderation's policy already takes), `IdempotencyTableWrite`, `FailureDlqSend`. Zero resource-ARN overlap with `moderation`/`dedupe` policies (ADR-AI-002), confirmed by inspection.
  - `iac/terraform/` root config now instantiates all of the above and wires them together (`module.enrichment_idempotency_table`, `module.lambda_enrichment`, `module.kafka_event_source_mapping`, plus the extended `module.iam_roles`/`module.review_queue` inputs) — same module-output-to-next-module-input style as the existing Moderation wiring. New root variable: `enrichment_lambda_package_path` (separate deployment package from Moderation's).
  - Small gap found and fixed during implementation: `lambda-enrichment`'s original `outputs.tf` (from its own task) didn't expose the resolved Kafka bootstrap address, which `kafka-event-source-mapping` needed as an input to avoid a second independent `terraform_remote_state` read of `rentifyx-platform` — added a `kafka_bootstrap_servers` output (same `try()`-wrapped SSM value the Lambda's own env var already uses).
  - Gate check: `terraform fmt -check -recursive` clean across all of `iac/`; `terraform validate` clean individually in all 7 modules under `iac/modules/` (`dynamodb-table`, `review-queue`, `iam-roles`, `lambda-enrichment`, `kafka-event-source-mapping`, `lambda-moderation`, `s3-trigger` — the last two re-validated as a regression check, unaffected) and in `iac/terraform/` root.
  - `terraform plan` at the root was attempted with placeholder values for the pre-existing unresolved variables (media bucket, package paths, Bedrock ARN) to check for any newly-introduced unresolved variables, but Terraform requires a real S3 backend init to run `plan`/`apply` even when `init -backend=false` was used for `validate` — same blocker prior sessions already hit for `apply`, not a new one. Not forced through without deploy authorization; `validate` remains the achievable gate in this environment, consistent with `.specs/project/STATE.md`'s existing note on `iac/terraform`'s TESTING.md gate command.
  - Two sub-agents (T1, T2, T3 in Phase 1) ran fully in parallel directly in the main working directory — no file overlap (three different module directories), no worktree isolation needed.

## Open Items

- Confirm the final .NET 10 SDK pin to use in CI and local development — `global.json` pins `10.0.100-preview.7` (rollForward `latestFeature`), but every build this session actually resolved `10.0.400-preview.0.26322.102` (a locally-installed higher feature band); need to decide whether to pin that exact version explicitly (and confirm it's installable via `actions/setup-dotnet` on `ubuntu-latest`, not just present locally) or wait for a stable 10.0 GA release. Product/deploy decision, not something to resolve unilaterally.
- S3 key convention (`assets/{ownerId}/{assetId}/{filename}`) still needs confirmation from the `asset-registry-api` team (gap `G-001`, tracked on both repos' `STATE.md`) — `iac/terraform`'s `filter_prefix`/`filter_suffix` variables default to `""` (no convention baked in) until it's confirmed.
- ADR-AI-007 (Dedupe, DEF-AI-001) still not written — written when that lands. ADR-AI-005/006 (Enrichment) done.
- ~~`iac/modules/{lambda-enrichment,kafka-event-source-mapping}` still empty~~ — closed by E-03b (2026-07-24): both modules built, plus a generic `dynamodb-table` module, an enrichment failure DLQ, and `iam-roles`' `enrichment` policy extended with S3/DynamoDB/SQS. See E-03b entry above.
- Moderation's idempotency table still has no backing DynamoDB resource — `iac/terraform`'s `idempotency_table_name`/`idempotency_table_arn` remain plain variables with no default. The generic `iac/modules/dynamodb-table` module built for Enrichment in E-03b could back it too, but adopting it for Moderation wasn't done (deliberately out of E-03b's scope).
- `iac/terraform` root config composes `iam-roles`/`review-queue`/`lambda-moderation`/`s3-trigger`/`enrichment_idempotency_table`/`lambda_enrichment`/`kafka_event_source_mapping` and `validate`s clean, but cannot `apply` (or even `plan`, discovered during E-03b — Terraform requires a real S3 backend init for `plan`/`apply`, `-backend=false` only works for `validate`) — `media_bucket_id`/`media_bucket_arn` (bucket doesn't exist anywhere), `lambda_package_path`/`enrichment_lambda_package_path` (no CI build step produces either zip yet), and `bedrock_model_arn` (no real ARN chosen — E-03 picked the model, `us.anthropic.claude-sonnet-5`, but not a concrete Bedrock resource/inference-profile ARN for the Terraform variable) all have no default and need real values before a real deploy or even a real `plan`.
- Bedrock region reachability unconfirmed: `us.anthropic.claude-sonnet-5` is a US cross-region inference profile; whether it's callable directly from `iac/terraform`'s `aws_region` default (`sa-east-1`) or needs explicit cross-region routing wasn't confirmed during E-03 (`.specs/features/e03-enrichment-pipeline/design.md`'s Open Questions).
- `AssetMediaModerated` v2 / `AssetEnrichmentSuggested` — `asset-registry-api`'s own consumers for these events don't exist yet, same situation `AssetMediaModerated` v1 was in when E-02 shipped it.
